# Documentation Index

## Canonical/current

- [`TEMPORAL_CONTRACT.md`](TEMPORAL_CONTRACT.md) — prediction-time information
  rules, timestamp ties, rolling windows, and the reproducible source audit.
- [`ENTITY_MODEL.md`](ENTITY_MODEL.md) — canonical entities, relationships,
  state grains, and source limitations.
- [`FEATURE_DICTIONARY_V1.md`](FEATURE_DICTIONARY_V1.md) — exact 30-feature PRD
  reference: definitions, dtypes, temporal/static classification, nullability,
  and missing behavior.
- `src/training/feature_contracts.py` freezes the 17- and 25-feature contracts
  separately from the current 30-feature contract. Historical reproduction
  always selects a frozen contract; current model claims use current results.
- [`../models/prd/prd_model_manifest.json`](../models/prd/prd_model_manifest.json) —
  machine-readable current model, artifact, feature, split, parameter, and
  evaluation contract. Technical fields are generated by
  `src/training/build_prd_manifest.py`; promotion is a human decision;
  `src/training/prd.py` validates the generated pointer.

## Decision and historical evidence

- [`MODEL_PROMOTION_GENOME_V1.md`](MODEL_PROMOTION_GENOME_V1.md) — why the Genome
  candidate was promoted, comparison evidence, artifact provenance, costs, and
  limitations.
- [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) — completed phases, major
  architectural decisions, deferred work, and the serving roadmap.
- [`history/PROJECT_CONTEXT.md`](history/PROJECT_CONTEXT.md) — superseded early
  project brief retained to explain the original enrichment-first direction and
  historical phase numbering.

Notebooks are the primary training and evaluation workflow;
`src/training/prd.py` validates the generated pointer without retraining. The
notebooks in `notebooks/model/phase5_baseline_17/` and original 17-feature
artifacts in `models/history/phase5/` are frozen Phase 5 evidence. The
canonical 30-feature workflow is `notebooks/model/genome_prd_v1/`; in order,
its notebooks validate the v2 dataset, audit temporal splits, train, evaluate,
and generate the PRD manifest. The two top-level notebooks are exploratory EDA
and are not production feature specifications. Canonical model and result
artifacts live in `models/prd/`; controlled selection evidence lives in
`models/experiments/`.
The immutable
`models/experiments/genome_experiment_v1/` directory must not be rewritten.
Frozen Phase 5 notebook cells and saved outputs retain their original
root-level `models/` paths as historical evidence; current code uses the
organized paths above.

A future whitepaper should synthesize these sources; it should not become a new
executable or feature-contract source of truth.
